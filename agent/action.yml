name: 'RepoKeeper Implementation Agent'
description: 'AI agent that reads issues, writes code, verifies, and opens reviewable PRs'
inputs:
  repo:
    description: 'GitHub repository as owner/name'
    required: true
  issue:
    description: 'GitHub issue number'
    required: true
  pr:
    description: 'GitHub PR number (when triggered by PR comment) — omit for issue trigger'
    required: false
    default: ''
  llm_api_key:
    description: 'LLM API key'
    required: false
  llm_base_url:
    description: 'OpenAI-compatible API base URL'
    required: false
    default: 'https://api.deepseek.com'
  github_token:
    description: 'GitHub token with repo scope'
    required: false

runs:
  using: 'composite'
  steps:
    - uses: actions/checkout@v6
      with:
        fetch-depth: 0

    - uses: actions/setup-python@v6
      with:
        python-version: '3.10'

    # Node.js + Pi are always installed so agent.backend: pi works out of the
    # box when configured in repokeeper.yml.  Adds ~20 s to every run.
    - uses: actions/setup-node@v6
      with:
        node-version: '20'

    - name: Install Pi
      shell: bash
      run: npm install -g @earendil-works/pi-coding-agent

    - name: Install RepoKeeper
      shell: bash
      run: pip install repokeeper

    - name: Run Implementation Agent
      shell: bash
      env:
        PYTHONUNBUFFERED: '1'
        DEEPSEEK_API_KEY: ${{ inputs.llm_api_key }}
        REPOKEEPER_GITHUB_TOKEN: ${{ inputs.github_token }}
        GITHUB_REPOSITORY: ${{ inputs.repo }}
        LLM_BASE_URL: ${{ inputs.llm_base_url }}
        PR_NUMBER: ${{ inputs.pr }}
      run: repokeeper agent --repo "${{ inputs.repo }}" --issue "${{ inputs.issue }}"
